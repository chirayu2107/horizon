'use server'

export const signIn = async () => {
    try{
        //Mutation / fetch data
    }
    catch (error){
        console.error('Error', error);
    }
}

export const signUp = async (userData : SignUpParams) => {
    try{
        //create user account
    }
    catch (error){
        console.error('Error', error);
    }
}